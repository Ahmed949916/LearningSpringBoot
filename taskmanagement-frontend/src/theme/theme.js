import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    primary: {
      main: '#004030',
      lightMain: '#4A9782',
      textLight:"#FFFFFF"
    },
    error: {
      main: '#f50057',
    },
  },
});